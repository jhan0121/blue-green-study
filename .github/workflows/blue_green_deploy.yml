name: Blue-Green Deployment Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment (auto/blue/green)'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - blue
          - green
      force_deploy:
        description: 'Force deployment even if tests fail'
        required: false
        default: false
        type: boolean

env:
  BUILD_NUMBER: ${{ github.run_number }}

jobs:
  test:
    name: 🧪 Test Application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'corretto'

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Run tests
        run: ./gradlew test

      - name: Build application
        run: ./gradlew clean build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: jar-artifact
          path: bluegreen/build/libs/*-SNAPSHOT.jar
          retention-days: 7

  deploy:
    name: 🚀 Deploy to Production
    runs-on: self-hosted
    needs: test
    if: github.ref == 'refs/heads/main' && (success() || github.event.inputs.force_deploy == 'true')

    outputs:
      current_env: ${{ steps.detect.outputs.current }}
      target_env: ${{ steps.detect.outputs.target }}
      deploy_success: ${{ steps.deploy_result.outputs.success }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: jar-artifact
          path: bluegreen/build/libs/

      - name: Set up environment variables
        run: |
          # .env 파일 생성
          echo "BUILD_NUMBER=${{ env.BUILD_NUMBER }}" > .env
          echo "MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}" >> .env
          echo "MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}" >> .env
          echo "MYSQL_USER=${{ secrets.MYSQL_USER }}" >> .env
          echo "MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}" >> .env

      - name: Detect current environment and determine target
        id: detect
        run: |
          set -e

          # 수동으로 환경이 지정된 경우
          if [ "${{ github.event.inputs.environment }}" != "auto" ] && [ "${{ github.event.inputs.environment }}" != "" ]; then
            TARGET="${{ github.event.inputs.environment }}"
            echo "🎯 Manual target environment: $TARGET"
          else
            # 현재 실행 중인 환경 자동 감지
            if docker ps --format "table {{.Names}}" | grep -q "app_blue" && curl -s http://localhost:3030/health | grep -i blue; then
              CURRENT="blue"
              TARGET="green"
            elif docker ps --format "table {{.Names}}" | grep -q "app_green" && curl -s http://localhost:3030/health | grep -i green; then
              CURRENT="green"
              TARGET="blue"
            else
              # 아무것도 실행 중이지 않거나 감지할 수 없으면 blue로 시작
              CURRENT="none"
              TARGET="blue"
            fi
            echo "🔍 Auto-detected - Current: $CURRENT → Target: $TARGET"
          fi

          echo "current=${CURRENT:-none}" >> $GITHUB_OUTPUT
          echo "target=$TARGET" >> $GITHUB_OUTPUT

      - name: Build Docker image
        run: |
          TARGET_ENV=${{ steps.detect.outputs.target }}
          echo "🏗️  Building Docker image: myapp:$TARGET_ENV"
          docker build -t myapp:$TARGET_ENV .

      - name: Pre-deployment health check
        run: |
          echo "🏥 Pre-deployment system health check"

          # Docker 상태 확인
          docker system df

          # 디스크 공간 확인
          df -h

          # 최소한 하나의 환경이 실행 중인지 확인
          if ! docker ps | grep -q "app_blue" && ! docker ps | grep -q "app_green"; then
            echo "🔧 Starting Blue environment first..."
            docker-compose up -d app_blue mysql_blue
            sleep 10
          fi

          # Nginx 컨테이너 확인 및 시작
          if docker ps -a | grep -q "nginx_lb"; then
            echo "🔧 Checking if Nginx needs volume update..."
            # upstream 파일이 마운트되었는지 확인
            if ! docker exec nginx_lb test -f /etc/nginx/upstream-blue.conf 2>/dev/null || \
               ! docker exec nginx_lb test -f /etc/nginx/upstream-green.conf 2>/dev/null; then
              echo "⚠️  Volume mounts outdated, recreating Nginx container..."
              docker-compose up -d --force-recreate nginx
              sleep 5
            else
              echo "✅ Nginx volume mounts are up to date"
            fi
          else
            echo "🔧 Starting Nginx load balancer"
            docker-compose up -d nginx
            sleep 5
          fi

          # 컨테이너가 완전히 실행될 때까지 대기
          echo "⏳ Waiting for Nginx container to be fully ready..."
          MAX_WAIT=30
          WAIT_COUNT=0
          while [ $WAIT_COUNT -lt $MAX_WAIT ]; do
            if docker ps | grep -q "nginx_lb" && [ "$(docker inspect -f '{{.State.Status}}' nginx_lb 2>/dev/null)" = "running" ]; then
              echo "✅ Nginx container is ready"
              break
            fi
            WAIT_COUNT=$((WAIT_COUNT + 1))
            echo "Waiting... ($WAIT_COUNT/$MAX_WAIT)"
            sleep 1
          done

          if [ $WAIT_COUNT -eq $MAX_WAIT ]; then
            echo "❌ Nginx container failed to become ready"
            docker logs nginx_lb --tail 20
            exit 1
          fi

          echo "✅ Nginx is running successfully"

      - name: Setup MySQL replication (if needed)
        run: |
          TARGET_ENV=${{ steps.detect.outputs.target }}
          CURRENT_ENV=${{ steps.detect.outputs.current }}

          if [ "$CURRENT_ENV" != "none" ] && [ "$TARGET_ENV" = "green" ]; then
            echo "🔄 Setting up MySQL replication for Green environment"

            # Green 컨테이너가 정지 상태이고 네트워크 문제가 있는 경우에만 정리
            if docker ps -a --filter "status=exited" --filter "name=mysql_green" --format "{{.Names}}" | grep -q "mysql_green"; then
              echo "Cleaning up stopped mysql_green container with potential network issues..."
              docker rm mysql_green 2>/dev/null || true
            fi
            if docker ps -a --filter "status=exited" --filter "name=app_green" --format "{{.Names}}" | grep -q "app_green"; then
              echo "Cleaning up stopped app_green container with potential network issues..."
              docker rm app_green 2>/dev/null || true
            fi

            # Green MySQL 시작 (이미 실행 중이면 스킵)
            if ! docker ps | grep -q "mysql_green"; then
              docker-compose --profile green up -d mysql_green
              sleep 10
            else
              echo "mysql_green is already running"
            fi

            # 복제 설정 실행
            chmod +x scripts/setup_replication.sh
            ./scripts/setup_replication.sh || echo "⚠️  Replication setup failed, continuing with deployment"
          fi

      - name: Deploy new environment
        id: deploy
        run: |
          set -e
          TARGET_ENV=${{ steps.detect.outputs.target }}
          CURRENT_ENV=${{ steps.detect.outputs.current }}

          echo "🚀 Deploying to $TARGET_ENV environment"

          # 환경별 배포
          export BUILD_NUMBER=${{ env.BUILD_NUMBER }}
          export MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}
          export MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}
          export MYSQL_USER=${{ secrets.MYSQL_USER }}
          export MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}

          if [ "$TARGET_ENV" = "green" ]; then
            # Green 환경 배포 (정지된 컨테이너만 정리)
            if docker ps -a --filter "status=exited" --filter "name=app_green" --format "{{.Names}}" | grep -q "app_green"; then
              echo "Removing stopped app_green container..."
              docker rm app_green 2>/dev/null || true
            fi
            if docker ps -a --filter "status=exited" --filter "name=mysql_green" --format "{{.Names}}" | grep -q "mysql_green"; then
              echo "Removing stopped mysql_green container..."
              docker rm mysql_green 2>/dev/null || true
            fi
            docker-compose --profile green up -d app_green mysql_green
          else
            # Blue 환경 배포 (정지된 컨테이너만 정리)
            if docker ps -a --filter "status=exited" --filter "name=app_blue" --format "{{.Names}}" | grep -q "app_blue"; then
              echo "Removing stopped app_blue container..."
              docker rm app_blue 2>/dev/null || true
            fi
            if docker ps -a --filter "status=exited" --filter "name=mysql_blue" --format "{{.Names}}" | grep -q "mysql_blue"; then
              echo "Removing stopped mysql_blue container..."
              docker rm mysql_blue 2>/dev/null || true
            fi
            docker-compose up -d app_blue mysql_blue
          fi

      - name: Wait for application readiness
        timeout-minutes: 10
        run: |
          TARGET_ENV=${{ steps.detect.outputs.target }}
          echo "⏳ Waiting for $TARGET_ENV environment to be ready..."

          MAX_ATTEMPTS=30
          ATTEMPT=0

          while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
            ATTEMPT=$((ATTEMPT + 1))

            if docker exec app_$TARGET_ENV curl -s -f http://localhost:8080/health >/dev/null 2>&1; then
              echo "✅ $TARGET_ENV environment is healthy!"

              # 추가 검증
              HEALTH_RESPONSE=$(docker exec app_$TARGET_ENV curl -s http://localhost:8080/health)
              VERSION_RESPONSE=$(docker exec app_$TARGET_ENV curl -s http://localhost:8080/version)

              echo "Health: $HEALTH_RESPONSE"
              echo "Version: $VERSION_RESPONSE"
              break
            fi

            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS: $TARGET_ENV not ready yet..."
            sleep 10
          done

          if [ $ATTEMPT -eq $MAX_ATTEMPTS ]; then
            echo "❌ $TARGET_ENV environment failed to become ready"
            exit 1
          fi

      - name: Switch traffic using deployment scripts
        run: |
          TARGET_ENV=${{ steps.detect.outputs.target }}

          echo "🔄 Switching traffic to $TARGET_ENV using deployment scripts"

          # 배포 스크립트 실행 권한 부여
          chmod +x scripts/*.sh

          # 환경에 따른 스크립트 실행
          if [ "$TARGET_ENV" = "green" ]; then
            ./scripts/switch_to_green.sh
          else
            ./scripts/switch_to_blue.sh
          fi

      - name: Post-deployment verification
        id: verify
        run: |
          TARGET_ENV=${{ steps.detect.outputs.target }}

          echo "🔍 Post-deployment verification"

          # 잠시 대기
          sleep 5

          # 외부에서 접근 가능한 엔드포인트 테스트
          MAX_RETRIES=10
          RETRY=0

          while [ $RETRY -lt $MAX_RETRIES ]; do
            RETRY=$((RETRY + 1))

            HEALTH_RESPONSE=$(curl -s http://localhost:3030/health || echo "FAILED")
            VERSION_RESPONSE=$(curl -s http://localhost:3030/version || echo "FAILED")

            if echo "$HEALTH_RESPONSE" | grep -i "$TARGET_ENV" && [ "$HEALTH_RESPONSE" != "FAILED" ]; then
              echo "✅ Verification successful!"
              echo "Health: $HEALTH_RESPONSE"
              echo "Version: $VERSION_RESPONSE"
              echo "success=true" >> $GITHUB_OUTPUT
              break
            fi

            echo "Retry $RETRY/$MAX_RETRIES: Verification failed, retrying..."
            sleep 3
          done

          if [ $RETRY -eq $MAX_RETRIES ]; then
            echo "❌ Post-deployment verification failed"
            echo "Health response: $HEALTH_RESPONSE"
            echo "Version response: $VERSION_RESPONSE"
            echo "success=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Cleanup old environment
        if: steps.verify.outputs.success == 'true'
        run: |
          TARGET_ENV=${{ steps.detect.outputs.target }}
          CURRENT_ENV=${{ steps.detect.outputs.current }}

          if [ "$CURRENT_ENV" != "none" ] && [ "$CURRENT_ENV" != "$TARGET_ENV" ]; then
            echo "🧹 Cleaning up old environment: $CURRENT_ENV"

            # 잠시 대기 후 정리
            sleep 10
            docker-compose stop app_$CURRENT_ENV mysql_$CURRENT_ENV || echo "⚠️  Cleanup warning: containers may have already been stopped"

            echo "✅ Cleanup completed"
          else
            echo "ℹ️  No cleanup needed"
          fi

      - name: Set deployment result
        id: deploy_result
        run: |
          if [ "${{ steps.verify.outputs.success }}" == "true" ]; then
            echo "success=true" >> $GITHUB_OUTPUT
          else
            echo "success=false" >> $GITHUB_OUTPUT
          fi

  integration-test:
    name: 🧪 Integration Tests
    runs-on: self-hosted
    needs: deploy
    if: needs.deploy.outputs.deploy_success == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run integration tests
        run: |
          echo "🧪 Running integration tests"

          # 통합 테스트 스크립트 실행
          chmod +x scripts/test_deployment.sh
          ./scripts/test_deployment.sh

      - name: Performance smoke test
        run: |
          echo "⚡ Running performance smoke test"

          # 간단한 부하 테스트
          for i in {1..10}; do
            curl -s http://localhost:3030/health >/dev/null
            curl -s http://localhost:3030/version >/dev/null
            sleep 0.1
          done

          echo "✅ Performance smoke test completed"

  rollback:
    name: 🔄 Rollback (if needed)
    runs-on: self-hosted
    needs: [ deploy, integration-test ]
    if: failure() && needs.deploy.outputs.current_env != 'none'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Execute rollback
        run: |
          CURRENT_ENV="${{ needs.deploy.outputs.current_env }}"

          if [ "$CURRENT_ENV" != "none" ]; then
            echo "🔄 Rolling back to $CURRENT_ENV environment"

            chmod +x scripts/*.sh

            if [ "$CURRENT_ENV" = "blue" ]; then
              ./scripts/switch_to_blue.sh
            else
              ./scripts/switch_to_green.sh
            fi

            echo "✅ Rollback completed"
          else
            echo "⚠️  No previous environment to rollback to"
          fi

      - name: Verify rollback
        run: |
          CURRENT_ENV="${{ needs.deploy.outputs.current_env }}"

          if [ "$CURRENT_ENV" != "none" ]; then
            sleep 5
            HEALTH_RESPONSE=$(curl -s http://localhost:3030/health)

            if echo "$HEALTH_RESPONSE" | grep -i "$CURRENT_ENV"; then
              echo "✅ Rollback verification successful: $HEALTH_RESPONSE"
            else
              echo "❌ Rollback verification failed: $HEALTH_RESPONSE"
              exit 1
            fi
          fi

  notify:
    name: 📢 Notify Results
    runs-on: self-hosted
    needs: [ deploy, integration-test ]
    if: always()

    steps:
      - name: Deployment Summary
        run: |
          echo "🎯 Deployment Summary"
          echo "===================="
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Build Number: ${{ env.BUILD_NUMBER }}"
          echo "Current Environment: ${{ needs.deploy.outputs.current_env }}"
          echo "Target Environment: ${{ needs.deploy.outputs.target_env }}"
          echo "Deploy Success: ${{ needs.deploy.outputs.deploy_success }}"
          echo "Integration Tests: ${{ needs.integration-test.result }}"
          echo ""

          if [ "${{ needs.deploy.outputs.deploy_success }}" == "true" ] && [ "${{ needs.integration-test.result }}" == "success" ]; then
            echo "🎉 Deployment completed successfully!"
          else
            echo "❌ Deployment encountered issues"
          fi
